import { useState } from 'react'; import RoleShell from '../components/RoleShell'; import SuperAdminDashboard from './SuperAdminDashboard'; import Assignment from './Assignment'; import Report from './Report'; import { IconTarget, IconList, IconReport } from '../components/Icons';
export default function SuperAdminApp(props){const[view,setView]=useState('overview');const nav=[{id:'overview',label:'Executive Overview',icon:IconTarget},{id:'assignment',label:'Workload Control',icon:IconList},{id:'reports',label:'Reports',icon:IconReport}];return <RoleShell roleLabel="Super Admin" nav={nav} view={view} setView={setView}>{view==='overview'?<SuperAdminDashboard {...props}/>:view==='assignment'?<Assignment {...props}/>:<Report {...props}/>}</RoleShell>}
