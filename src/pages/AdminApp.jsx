import { useState } from 'react'; import RoleShell from '../components/RoleShell'; import AdminDashboard from './AdminDashboard'; import Assignment from './Assignment'; import InvoiceWorkspace from './InvoiceWorkspace'; import { IconTarget, IconList, IconUsers } from '../components/Icons';
export default function AdminApp(props){const[view,setView]=useState('dashboard');const[selected,setSelected]=useState(null);const nav=[{id:'dashboard',label:'Lead Dashboard',icon:IconTarget},{id:'assignment',label:'Assign Work',icon:IconList},{id:'officers',label:'Officer Performance',icon:IconUsers}];if(selected)return <RoleShell roleLabel="Verification Lead / Admin" nav={nav} view={view} setView={setView}><InvoiceWorkspace record={selected} officers={props.officers} actorRole="admin" actorName="Verification Lead" onClose={()=>setSelected(null)} onRefresh={props.setReceptions}/></RoleShell>;return <RoleShell roleLabel="Verification Lead / Admin" nav={nav} view={view} setView={setView}>{view==='dashboard'?<AdminDashboard {...props}/>:view==='assignment'?<Assignment {...props}/>:<AdminDashboard {...props}/>}</RoleShell>}
