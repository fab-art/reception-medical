import { useState } from 'react'; import RoleShell from '../components/RoleShell'; import NewReception from './NewReception'; import { IconPlus, IconList } from '../components/Icons'; import { Card } from '../components/UI';
export default function DistrictOfficerApp(props){const[view,setView]=useState('new'); const nav=[{id:'new',label:'Submit to HQ',icon:IconPlus},{id:'records',label:'My submissions',icon:IconList}]; const records=props.receptions.filter(r=>r.receivedByName==='District Office'); return <RoleShell roleLabel={`District · ${props.officer?.name||''}`} nav={nav} view={view} setView={setView}>{view==='new'?<NewReception {...props} districtMode officer={props.officer}/>:<div className="p-8"><h1 className="text-2xl font-semibold text-rssb-blue-dark mb-4">District submissions</h1><Card className="p-4"><div className="text-sm text-gray-500">{records.length} submissions</div></Card></div>}</RoleShell>}
