import { useState } from 'react'; import { Button, Field, inputCls } from '../components/UI'; import { resolveLogin,setSession } from '../lib/auth'; import { getOfficers,getSettings } from '../lib/db'; import logo from '../assets/rssb-logo.png';
export default function Landing(){const[v,setV]=useState('');const[e,setE]=useState('');const[busy,setBusy]=useState(false);async function submit(x){x.preventDefault();setBusy(true);setE('');const[s,o]=await Promise.all([getSettings(),getOfficers()]);let session=resolveLogin(v,s,o);const officer=session?.officerId?o.find(a=>a.id===session.officerId):null;if(session&&officer?.isDistrictOfficer)session={...session,role:'district'};if(!session)e('That password or PIN was not recognized.');else{setSession(session);location.reload()}setBusy(false)}return <div className="min-h-screen flex items-center justify-center bg-gradient-to-br from-[#eef1f6] to-rssb-blue-light p-4"><div className="w-full max-w-md"><div className="text-center mb-8"><img src={logo} className="h-16 mx-auto mb-3"/><h1 className="font-display text-2xl font-semibold text-rssb-blue-dark">Medical Invoice Workflow System</h1><p className="text-sm text-gray-500 mt-1">District submission · HQ reception · verification · Finance handoff</p></div><form onSubmit={submit} className="bg-white p-6 rounded-2xl shadow-lg border"><Field label="Password or PIN"><input autoFocus type="password" className={`${inputCls} text-center text-lg tracking-widest`} value={v} onChange={x=>setV(x.target.value)} placeholder="Enter password or PIN"/></Field>{e&&<div className="text-sm text-rose-600 bg-rose-50 p-2 rounded mb-3">{e}</div>}<Button className="w-full" disabled={busy||!v}>{busy?'Signing in...':'Continue'}</Button></form><div className="mt-5 text-xs text-gray-400 space-y-1"><div>Demo roles: Reception <b>reception123</b> · Admin <b>admin123</b> · Super Admin <b>superadmin123</b></div><div>Verification officers use their generated 4-digit PIN. District demo: <b>9003</b>.</div></div></div></div>}
