import { useMemo } from 'react'; import { Card, StatCard } from '../components/UI'; import { IconAlert, IconCheck, IconList, IconMoney } from '../components/Icons'; import { formatMoney } from '../lib/utils'; import { invoiceMetrics } from '../lib/metrics'; import { SLA_DAYS, ageDays } from '../lib/workflow';
export default function AdminDashboard({receptions=[],officers=[]}){const m=useMemo(()=>invoiceMetrics(receptions,officers),[receptions,officers]); const delayed=useMemo(()=>receptions.filter(r=>!r.verifiedAt&&ageDays(r.submittedToHqAt||r.receivedAt)>SLA_DAYS).slice(0,10),[receptions]);return <div className="p-4 md:p-8 max-w-7xl"><header className="mb-6"><h1 className="font-display text-2xl md:text-3xl font-semibold text-rssb-blue-dark">Verification Lead Control Center</h1><p className="text-sm text-gray-500 mt-1">Assign workload, protect the 15-day verification SLA, and identify where delays are accumulating.</p></header><div className="grid grid-cols-2 md:grid-cols-6 gap-3 mb-6"><StatCard label="Open" value={m.open.length} sub="not yet verified" accent="blue" icon={IconList}/><StatCard label="Ready to assign" value={receptions.filter(r=>r.status==='ready_for_assignment').length} sub="clean reception records" accent="teal" icon={IconCheck}/><StatCard label="Over 15 days" value={m.overdue} sub="SLA breaches" accent="red" icon={IconAlert}/><StatCard label="SLA met" value={`${m.slaRate}%`} sub="of completed verification" accent="teal" icon={IconCheck}/><StatCard label="Billed" value={formatMoney(m.billed)} sub="RWF all records" accent="gold" icon={IconMoney}/><StatCard label="Deduction" value={formatMoney(m.deductions)} sub="identified in verification" accent="gold" icon={IconMoney}/></div><div className="grid lg:grid-cols-2 gap-4 mb-6"><Card className="p-5"><h2 className="font-semibold mb-4">Pipeline</h2><div className="space-y-4">{[['HQ reception',m.stageCounts.hq_reception],['Verification',m.stageCounts.verification],['Finance / paid',m.stageCounts.finance]].map(([k,v])=><div key={k}><div className="flex justify-between text-sm mb-1"><span>{k}</span><span className="font-semibold">{v}</span></div><div className="h-2 bg-gray-100 rounded-full overflow-hidden"><div className="h-full bg-rssb-blue" style={{width:`${m.total?Math.round(v/m.total*100):0}%`}}/></div></div>)}</div></Card><Card className="p-5"><h2 className="font-semibold mb-4">Officer workload & performance</h2><div className="overflow-x-auto"><table className="w-full text-sm"><thead className="text-xs uppercase text-gray-400"><tr><th className="text-left">Officer</th><th className="text-right">Open</th><th className="text-right">Done</th><th className="text-right">Voucher %</th><th className="text-right">SLA</th></tr></thead><tbody className="divide-y">{m.officerStats.slice(0,10).map(s=><tr key={s.officer.id}><td className="py-2">{s.officer.name}</td><td className="text-right">{s.pending}</td><td className="text-right">{s.completed}</td><td className="text-right">{s.voucherRate}%</td><td className="text-right">{s.breaches===0?'On track':`${s.breaches} breach`}</td></tr>)}</tbody></table></div></Card></div><Card className="overflow-x-auto"><div className="px-4 py-3 border-b font-semibold">Overdue invoices — intervene by root-cause</div><table className="w-full text-sm"><thead className="bg-gray-50 text-xs uppercase text-gray-500"><tr><th className="text-left px-4 py-2">Invoice</th><th className="text-left px-4 py-2">Facility</th><th className="text-left px-4 py-2">Stage</th><th className="text-right px-4 py-2">Age</th><th className="text-left px-4 py-2">Delay reason</th></tr></thead><tbody className="divide-y">{delayed.map(r=><tr key={r.id}><td className="px-4 py-2 font-mono text-xs">{r.receiptNo}</td><td className="px-4 py-2">{r.facilityName}</td><td className="px-4 py-2">{r.status.replaceAll('_',' ')}</td><td className="px-4 py-2 text-right text-rose-600">{ageDays(r.submittedToHqAt||r.receivedAt)}d</td><td className="px-4 py-2">{r.delayReason||'No reason captured'}</td></tr>)}{!delayed.length&&<tr><td colSpan="5" className="py-8 text-center text-gray-400">No current SLA breaches.</td></tr>}</tbody></table></Card></div>}
