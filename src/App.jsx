import { useEffect,useState } from 'react'; import { getFacilities,getReceptions,getSettings,getOfficers,initializeStore } from './lib/db'; import { getSession } from './lib/auth'; import Landing from './pages/Landing'; import ReceptionApp from './pages/ReceptionApp'; import AdminApp from './pages/AdminApp'; import SuperAdminApp from './pages/SuperAdminApp'; import OfficerPortal from './pages/OfficerPortal'; import DistrictOfficerApp from './pages/DistrictOfficerApp';
export default function App(){const[loading,setLoading]=useState(true);const[data,setData]=useState({facilities:[],receptions:[],officers:[],settings:null});const session=getSession();async function refresh(){await initializeStore();const [facilities,receptions,officers,settings]=await Promise.all([getFacilities(),getReceptions(),getOfficers(),getSettings()]);setData({facilities,receptions,officers,settings});setLoading(false)}useEffect(()=>{refresh()},[]);if(loading||!data.settings)return <div className="h-screen flex items-center justify-center text-rssb-blue font-semibold">Loading Invoice Workflow System...</div>;if(!session)return <Landing/>;const props={...data,setReceptions:refresh,setOfficers:refresh,setSettings:refresh};const officer=data.officers.find(o=>o.id===session.officerId);if(session.role==='district')return <DistrictOfficerApp {...props} officer={officer}/>;if(session.role==='reception')return <ReceptionApp {...props} officer={officer}/>;if(session.role==='officer')return <OfficerPortal {...props} officer={officer}/>;if(session.role==='admin')return <AdminApp {...props}/>;return <SuperAdminApp {...props}/>}
